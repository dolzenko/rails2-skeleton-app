%h1 Editing post
- form_for(@post) do |f|
  = f.error_messages
  %div
    = f.input :title, :label => 'Title'
    = f.input :body, :label => 'Body'
    = f.input :publication_state, :label => 'Publication state', :as => :radio
    = f.input :allow_comments, :label => 'Allow comments' 
    = f.submit 'Update'
= link_to 'Show', @post
|
\#{link_to 'Back', posts_path}
